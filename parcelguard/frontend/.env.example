# ParcelGuard — Frontend Environment Variables
# Copy to .env.local (not committed to git)
# cp .env.example .env.local

# API base URL — the Vite proxy handles /api → localhost:8000
# Only change this if your backend runs on a different host/port
VITE_API_URL=http://localhost:8000
